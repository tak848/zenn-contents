[settings]
idiomatic_version_file_enable_tools = ["node"]
auto_install = false
experimental = true
lockfile = true
locked = false                                 # aqua backend 等で checksum 未対応のため

[tools]
node = "24.12.0"
pnpm = "10.27.0"
"aqua:go-task/task" = "3.46.4"
"aqua:google/go-jsonnet" = "0.21.0"
