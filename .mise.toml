[settings]
idiomatic_version_file_enable_tools = ["node"]
auto_install = false
experimental = true
lockfile = true
locked = false                                 # aqua backend 等で checksum 未対応のため

[tools]
node = "24.20.0"
pnpm = "10.34.5"
"aqua:go-task/task" = "3.53.1"
"aqua:google/go-jsonnet" = "0.22.0"
